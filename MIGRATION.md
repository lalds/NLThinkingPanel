# 🔄 Миграция на NLThinkingPanel Pro

## Обзор изменений

Ваш бот был полностью переработан с нуля в профессиональную модульную систему. Старый файл `bot.py` сохранён, но теперь используется новый `main.py` с расширенной функциональностью.

## Что нового?

### ✅ Добавлено

1. **Модульная архитектура**
   - Разделение на слои (Core, Modules, Cogs, Config)
   - Легко расширяемая система

2. **Расширенные возможности**
   - Smart Cache с TTL
   - Rate Limiting
   - Детальная аналитика
   - История сообщений
   - Fallback модели AI
   - Retry logic

3. **Новые команды**
   - `!quick` - быстрые вопросы без контекста
   - `!context` - просмотр контекста
   - `!stats` - статистика (админ)
   - `!report` - отчёты (админ)
   - `!mystats` - личная статистика
   - `!config` - просмотр конфигурации (админ)
   - `!clearcache` - очистка кэша (админ)
   - `!clearhistory` - очистка истории (админ)
   - `!resetlimit` - сброс лимитов (админ)

4. **Улучшения AI**
   - Автоматическое переключение на резервные модели
   - Оптимизация длинных промптов
   - Кэширование ответов
   - Оценка токенов

5. **Мониторинг**
   - Цветные логи с ротацией
   - Детальная аналитика использования
   - Метрики производительности

### 🔄 Изменено

1. **Точка входа**
   - Старый: `python bot.py`
   - Новый: `python main.py`

2. **Конфигурация**
   - Добавлены новые параметры в `.env`
   - Валидация конфигурации при запуске

3. **Команды**
   - `!ask` теперь включает историю сообщений
   - Добавлены метаданные в ответы (модель, время)

## Шаги миграции

### Шаг 1: Обновите зависимости

```bash
pip install -r requirements.txt
```

### Шаг 2: Обновите .env файл

Скопируйте новые параметры из `.env.example`:

```env
# Новые параметры
MAX_TOKENS=2000
TEMPERATURE=0.7
MAX_HISTORY_MESSAGES=10
CONTEXT_WINDOW_HOURS=24
CACHE_ENABLED=true
CACHE_TTL_SECONDS=300
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=5
RATE_LIMIT_WINDOW=60
ANALYTICS_ENABLED=true
LOG_LEVEL=INFO
ADMIN_IDS=ваш_discord_id
ENABLED_MODULES=context,history,analytics,moderation
```

### Шаг 3: Запустите новую версию

```bash
python main.py
```

### Шаг 4: Проверьте работу

1. Проверьте логи в консоли - должны быть цветные
2. Проверьте создание директорий `logs/` и `data/`
3. Протестируйте команды:
   ```
   !ask Привет!
   !quick Что такое Python?
   !context
   !mystats
   ```

## Обратная совместимость

### Команда `!ask`

**Старая версия:**
```
!ask Кто играет?
```

**Новая версия (работает так же):**
```
!ask Кто играет?
```

Но теперь с дополнительными возможностями:
- История сообщений в контексте
- Кэширование ответов
- Метаданные в ответе
- Оптимизация промптов

### Конфигурация

Все старые параметры `.env` работают:
- `DISCORD_TOKEN`
- `OPENROUTER_API_KEY`
- `OPENROUTER_MODEL`
- `SYSTEM_PROMPT`
- `COMMAND_PREFIX`

Новые параметры имеют значения по умолчанию.

## Откат на старую версию

Если что-то пошло не так, вы можете вернуться к старой версии:

```bash
python bot.py
```

Старый файл `bot.py` сохранён без изменений.

## Проверка миграции

### Контрольный список

- [ ] Обновлены зависимости (`pip install -r requirements.txt`)
- [ ] Обновлён `.env` файл с новыми параметрами
- [ ] Бот запускается без ошибок (`python main.py`)
- [ ] Создались директории `logs/` и `data/`
- [ ] Команда `!ask` работает
- [ ] Команда `!quick` работает
- [ ] Команда `!mystats` показывает статистику
- [ ] Логи пишутся в файлы
- [ ] Админ-команды работают (если вы админ)

### Ожидаемый вывод при запуске

```
==================================================
🤖 Бот запущен: YourBotName (ID: ...)
📊 Серверов: 1
👥 Пользователей: 42
🔧 Префикс команд: !
🤖 Модель: anthropic/claude-3-haiku
📦 Модулей загружено: 2
==================================================
```

## Новые возможности для администраторов

### 1. Добавьте свой Discord ID в .env

```env
ADMIN_IDS=123456789,987654321
```

Теперь вы можете использовать админ-команды!

### 2. Мониторинг

Проверяйте статистику:
```
!stats
```

Генерируйте отчёты:
```
!report 7
```

### 3. Управление

Очистка кэша:
```
!clearcache
```

Сброс лимитов:
```
!resetlimit @User
```

## Часто задаваемые вопросы

### Q: Нужно ли удалять старый bot.py?

**A:** Нет, он сохранён для обратной совместимости. Можете оставить его или удалить.

### Q: Где хранятся логи?

**A:** В директории `logs/`. Создаётся автоматически при первом запуске.

### Q: Где хранится аналитика?

**A:** В файле `data/analytics.json`. Создаётся автоматически.

### Q: Как отключить кэширование?

**A:** В `.env` установите `CACHE_ENABLED=false`

### Q: Как отключить rate limiting?

**A:** В `.env` установите `RATE_LIMIT_ENABLED=false`

### Q: Как изменить лимит запросов?

**A:** В `.env` измените `RATE_LIMIT_REQUESTS=10` (например, на 10)

### Q: Бот не видит статусы пользователей

**A:** Проверьте, что в Discord Developer Portal включены все три Privileged Intents.

### Q: Как добавить новую команду?

**A:** Создайте файл в `cogs/` по примеру из `ARCHITECTURE.md`

### Q: Можно ли использовать другую модель?

**A:** Да, измените `OPENROUTER_MODEL` в `.env` на любую доступную модель OpenRouter.

## Поддержка

Если возникли проблемы:

1. Проверьте логи в `logs/`
2. Убедитесь, что `.env` заполнен правильно
3. Проверьте, что все Intents включены в Discord Developer Portal
4. Создайте Issue на GitHub с описанием проблемы

## Заключение

Поздравляем! 🎉 Ваш бот теперь:
- ✅ Модульный и расширяемый
- ✅ С кэшированием и оптимизацией
- ✅ С детальной аналитикой
- ✅ С защитой от спама
- ✅ С профессиональным логированием
- ✅ Готов к коммерческому использованию

Наслаждайтесь новыми возможностями! 🚀
