# 📦 Компоненты проекта NLThinkingPanel Pro

## Файловая структура

```
NLThinkingPanel/
│
├── 📄 main.py                    # Точка входа приложения
├── 📄 bot.py                     # Старая версия (deprecated)
├── 📄 requirements.txt           # Зависимости Python
├── 📄 .env                       # Конфигурация (не в Git)
├── 📄 .env.example               # Шаблон конфигурации
├── 📄 .gitignore                 # Игнорируемые файлы
│
├── 📖 README.md                  # Основная документация
├── 📖 ARCHITECTURE.md            # Архитектура проекта
├── 📖 EXAMPLES.md                # Примеры использования
├── 📖 MIGRATION.md               # Руководство по миграции
├── 📖 COMPONENTS.md              # Этот файл
│
├── 📁 config/                    # Конфигурация
│   ├── __init__.py
│   └── config.py                 # Централизованная конфигурация
│
├── 📁 core/                      # Ядро системы
│   ├── __init__.py
│   ├── logger.py                 # Система логирования
│   ├── cache.py                  # Smart Cache с TTL
│   └── rate_limiter.py           # Rate limiting
│
├── 📁 modules/                   # Бизнес-логика
│   ├── __init__.py
│   ├── ai_provider.py            # AI провайдер
│   ├── context_builder.py        # Построение контекста
│   └── analytics.py              # Аналитика
│
├── 📁 cogs/                      # Команды бота
│   ├── __init__.py
│   ├── ai_commands.py            # AI команды
│   └── admin.py                  # Админ команды
│
├── 📁 data/                      # Данные (создаётся автоматически)
│   └── analytics.json            # Статистика
│
└── 📁 logs/                      # Логи (создаётся автоматически)
    └── nlthinkingpanel_*.log     # Файлы логов
```

## Описание компонентов

### 🎯 Точка входа

#### `main.py`
**Назначение:** Главный файл запуска бота

**Функции:**
- Инициализация бота
- Валидация конфигурации
- Загрузка Cogs (расширений)
- Обработка глобальных событий
- Фоновые задачи (очистка кэша)

**Использование:**
```bash
python main.py
```

---

### ⚙️ Config Layer

#### `config/config.py`
**Назначение:** Централизованная система конфигурации

**Классы:**
- `BotConfig` - dataclass с настройками

**Возможности:**
- Загрузка из `.env`
- Валидация параметров
- Типизация значений
- Значения по умолчанию

**Параметры:**
- Discord настройки (токен, префикс)
- OpenRouter настройки (ключ, модель)
- AI параметры (температура, токены)
- Кэширование (включено, TTL)
- Rate limiting (лимиты, окно)
- Аналитика (включена, уровень логов)
- Администраторы (список ID)

---

### 🔧 Core Layer

#### `core/logger.py`
**Назначение:** Профессиональная система логирования

**Классы:**
- `ColoredFormatter` - цветной форматтер для консоли

**Функции:**
- `setup_logger()` - настройка логгера

**Возможности:**
- Цветной вывод в консоль
- Запись в файлы с ротацией
- Разные уровни логирования
- Автоматическая ротация (10MB)
- Хранение последних 5 файлов

**Использование:**
```python
from core.logger import logger

logger.info("Информационное сообщение")
logger.warning("Предупреждение")
logger.error("Ошибка")
```

---

#### `core/cache.py`
**Назначение:** Умное кэширование с TTL

**Классы:**
- `CacheEntry` - запись в кэше
- `SmartCache` - основной класс кэша

**Возможности:**
- Thread-safe реализация
- TTL (Time To Live)
- Автоматическая очистка
- Статистика (hits, misses, hit rate)
- Генерация уникальных ключей

**Использование:**
```python
from core.cache import cache

# Проверка кэша
cached = cache.get(arg1, arg2, kwarg1=value1)

# Сохранение в кэш
cache.set(result, arg1, arg2, kwarg1=value1, ttl=300)

# Статистика
stats = cache.get_stats()
```

---

#### `core/rate_limiter.py`
**Назначение:** Защита от спама

**Классы:**
- `RateLimiter` - sliding window rate limiter

**Возможности:**
- Sliding window алгоритм
- Настраиваемые лимиты
- Thread-safe
- Информация о времени сброса
- Админ-функции

**Использование:**
```python
from core.rate_limiter import rate_limiter

# Проверка лимита
if rate_limiter.is_allowed(user_id):
    # Обработка запроса
    pass
else:
    # Лимит превышен
    reset_time = rate_limiter.get_reset_time(user_id)
```

---

### 🧩 Modules Layer

#### `modules/ai_provider.py`
**Назначение:** Взаимодействие с AI моделями

**Классы:**
- `AIProvider` - провайдер AI

**Возможности:**
- Интеграция с OpenRouter
- Retry logic
- Fallback модели
- Оптимизация промптов
- Оценка токенов
- Кэширование ответов

**Методы:**
- `generate_response()` - генерация ответа
- `optimize_prompt()` - оптимизация промпта
- `estimate_tokens()` - оценка токенов

**Использование:**
```python
from modules.ai_provider import ai_provider

result = ai_provider.generate_response(
    system_prompt="Ты помощник",
    user_message="Привет!",
    use_cache=True
)
```

---

#### `modules/context_builder.py`
**Назначение:** Построение контекста для AI

**Классы:**
- `ContextBuilder` - построитель контекста

**Возможности:**
- Отслеживание активности пользователей
- История сообщений
- Статистика сервера
- Форматирование активностей

**Методы:**
- `build_full_context()` - полный контекст
- `build_user_context()` - контекст пользователей
- `get_message_history()` - история сообщений
- `add_message()` - добавление сообщения

**Использование:**
```python
from modules.context_builder import context_builder

# Добавление сообщения
context_builder.add_message(
    channel_id=123,
    author="User",
    content="Hello"
)

# Построение контекста
context = context_builder.build_full_context(
    guild=guild,
    channel_id=123,
    author_name="User",
    system_prompt="Ты помощник"
)
```

---

#### `modules/search_engine.py`
**Назначение:** Поиск информации в Интернете

**Классы:**
- `SearchEngine` - обертка над DuckDuckGo Search

**Возможности:**
- Анонимный поиск без использования API-ключей
- Кэширование результатов поиска (30 минут)
- Форматирование данных для передачи в ИИ

**Использование:**
```python
from modules.search_engine import search_engine

results = search_engine.search("новости технологий")
text_block = search_engine.format_results_for_ai(results)
```

---

#### `modules/analytics.py`
**Назначение:** Сбор и анализ метрик

**Классы:**
- `Analytics` - система аналитики

**Возможности:**
- Сбор метрик использования
- Статистика по пользователям
- Статистика по моделям
- Дневная статистика
- Логирование ошибок
- Генерация отчётов

**Методы:**
- `log_request()` - логирование запроса
- `log_error()` - логирование ошибки
- `get_stats()` - общая статистика
- `get_user_stats()` - статистика пользователя
- `get_daily_report()` - дневной отчёт

**Использование:**
```python
from modules.analytics import analytics

# Логирование запроса
analytics.log_request(
    user_id=123,
    user_name="User",
    model="claude-3-haiku",
    tokens_used=150,
    response_time=1.2
)

# Получение статистики
stats = analytics.get_stats()
```

---

### 🎮 Cogs Layer

#### `cogs/ai_commands.py`
**Назначение:** Основные AI команды

**Классы:**
- `AICog` - Cog с AI командами

**Команды:**
- `!ask <вопрос>` - вопрос с контекстом
- `!quick <вопрос>` - быстрый вопрос
- `!context` - показать контекст

**События:**
- `on_message` - отслеживание сообщений

**Возможности:**
- Rate limiting
- Кэширование
- Аналитика
- Разбивка длинных ответов
- Оптимизация промптов

---

#### `cogs/admin.py`
**Назначение:** Административные команды

**Классы:**
- `AdminCommands` - Cog с админ командами

**Команды:**
- `!stats` - общая статистика
- `!report [дни]` - отчёт за период
- `!config` - конфигурация
- `!clearcache` - очистка кэша
- `!clearhistory` - очистка истории
- `!resetlimit [@user]` - сброс лимита
- `!mystats` - личная статистика

**Возможности:**
- Проверка прав администратора
- Детальная статистика
- Управление системой

---

## Зависимости

### Основные

```
discord.py>=2.3.0      # Discord API
python-dotenv>=1.0.0   # Загрузка .env
openai>=1.12.0         # OpenRouter API
```

### Опциональные

```
aiohttp>=3.9.0         # Асинхронные HTTP запросы
```

---

## Данные

### `data/analytics.json`

**Структура:**
```json
{
  "total_requests": 1247,
  "total_tokens_used": 342891,
  "requests_by_user": {
    "123456789": {
      "name": "User",
      "count": 234,
      "tokens": 67891,
      "avg_response_time": 1.45
    }
  },
  "requests_by_model": {
    "anthropic/claude-3-haiku": 1100,
    "openai/gpt-3.5-turbo": 147
  },
  "daily_stats": {
    "2026-02-12": {
      "requests": 187,
      "tokens": 52341,
      "unique_users": ["123456789", "987654321"]
    }
  },
  "errors": [
    {
      "timestamp": "2026-02-12T15:30:00",
      "type": "ask_command",
      "message": "API Error",
      "user_id": 123456789
    }
  ],
  "start_time": "2026-02-07T10:00:00"
}
```

---

## Логи

### Формат файлов

```
logs/nlthinkingpanel_20260212.log
```

### Формат записей

```
2026-02-12 15:30:45 | INFO     | NLThinkingPanel | ask:82 | Запрос от User: Привет!...
2026-02-12 15:30:47 | INFO     | NLThinkingPanel | generate_response:84 | Успешный ответ от claude-3-haiku
```

---

## Конфигурация

### `.env` параметры

**Discord:**
- `DISCORD_TOKEN` - токен бота
- `COMMAND_PREFIX` - префикс команд (по умолчанию: `!`)

**OpenRouter:**
- `OPENROUTER_API_KEY` - API ключ
- `OPENROUTER_MODEL` - модель (по умолчанию: `anthropic/claude-3-haiku`)

**AI:**
- `SYSTEM_PROMPT` - системный промпт
- `MAX_TOKENS` - максимум токенов (по умолчанию: `2000`)
- `TEMPERATURE` - температура (по умолчанию: `0.7`)

**Контекст:**
- `MAX_HISTORY_MESSAGES` - размер истории (по умолчанию: `10`)
- `CONTEXT_WINDOW_HOURS` - окно контекста (по умолчанию: `24`)

**Кэш:**
- `CACHE_ENABLED` - включить кэш (по умолчанию: `true`)
- `CACHE_TTL_SECONDS` - TTL кэша (по умолчанию: `300`)

**Rate Limiting:**
- `RATE_LIMIT_ENABLED` - включить (по умолчанию: `true`)
- `RATE_LIMIT_REQUESTS` - лимит запросов (по умолчанию: `5`)
- `RATE_LIMIT_WINDOW` - окно в секундах (по умолчанию: `60`)

**Аналитика:**
- `ANALYTICS_ENABLED` - включить (по умолчанию: `true`)
- `LOG_LEVEL` - уровень логов (по умолчанию: `INFO`)

**Администрирование:**
- `ADMIN_IDS` - список Discord ID через запятую

**Модули:**
- `ENABLED_MODULES` - включенные модули (по умолчанию: `context,history,analytics,moderation`)

---

## Быстрый старт

1. **Установка:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Конфигурация:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env
   ```

3. **Запуск:**
   ```bash
   python main.py
   ```

---

## Документация

- **README.md** - основная документация
- **ARCHITECTURE.md** - архитектура проекта
- **EXAMPLES.md** - примеры использования
- **MIGRATION.md** - миграция со старой версии
- **COMPONENTS.md** - описание компонентов (этот файл)

---

## Поддержка

- 🐛 Баги: GitHub Issues
- 💡 Идеи: GitHub Discussions
- 📧 Email: support@example.com

---

*NLThinkingPanel Pro - профессиональный Discord бот с AI* 🤖✨
